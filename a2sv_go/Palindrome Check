package main

import "fmt"

func word_freq(word string) bool {
	var ch string
	for i := len(word) - 1; i >= 0; i-- {
		ch += string(word[i])
	}
	if ch == word {
		return true
	} else {
		return false
	}
}

func main() {
	fmt.Println(word_freq("h?e33e?h"))
}
